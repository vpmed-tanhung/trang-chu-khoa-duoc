const D=window.VPMED_DRUGS||[],$=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));$('#drug').innerHTML=D.map(x=>`<option value="${x.id}">${esc(x.brand)} — ${esc(x.active)}</option>`).join('');const KEY='vpmed_dose_history_v3';function load(){try{return JSON.parse(localStorage.getItem(KEY)||'[]')}catch{return[]}}function save(a){localStorage.setItem(KEY,JSON.stringify(a.slice(0,100)))}function renderHist(){const h=load();$('#hist').innerHTML=h.map(x=>`<tr><td>${esc(x.time)}</td><td>${esc(x.patient)}</td><td>${esc(x.crcl)} mL/ph</td><td>${esc(x.drug)}</td><td>${esc(x.advice)}</td></tr>`).join('')||'<tr><td colspan="5" style="text-align:center">Chưa có lịch sử</td></tr>'}renderHist();$('#clear').onclick=()=>{if(confirm('Xóa lịch sử trên thiết bị này?')){save([]);renderHist()}};$('#calc').onclick=()=>{const age=+$('#age').value,wt=+$('#wt').value,scru=+$('#scr').value;if(!age||!wt||!scru){alert('Vui lòng nhập tuổi, cân nặng và creatinine.');return}const scr=scru/88.4;let crcl=((140-age)*wt)/(72*scr);if($('#sex').value==='f')crcl*=.85;crcl=Math.max(0,crcl);const d=D.find(x=>x.id==$('#drug').value);const advice=$('#dialysis').checked?`HD: ${d.hd} | CRRT: ${d.crrt}`:`${d.renal.join(' ')}`;$('#output').className='calcresult show';$('#output').innerHTML=`<section class="card" style="margin-top:0"><div class="kicker">Kết quả Cockcroft–Gault</div><h2>${crcl.toFixed(1)} mL/phút</h2><p><b>${esc(d.brand)}</b> — ${esc(d.active)} (${esc(d.strength)})</p><div class="twocol"><div class="box"><h3>Liều tham khảo</h3><p>${esc(d.standard)}</p><h3>Gợi ý theo thận</h3>${d.renal.map(x=>`<p>• ${esc(x)}</p>`).join('')}</div><div class="box"><h3>HD</h3><p>${esc(d.hd)}</p><h3>CRRT</h3><p>${esc(d.crrt)}</p><h3>TDM</h3><p>${esc(d.tdm)}</p></div></div><div class="warn" style="margin-top:14px">Kết quả hỗ trợ tra cứu, không thay thế đánh giá lâm sàng. Đối chiếu tờ HDSD, chỉ định, vi sinh, cân nặng dùng để tính liều và quy trình bệnh viện.</div></section>`;const h=load();h.unshift({time:new Date().toLocaleString('vi-VN'),patient:$('#pn').value||'Không ghi tên',crcl:crcl.toFixed(1),drug:d.brand,advice});save(h);renderHist()};
