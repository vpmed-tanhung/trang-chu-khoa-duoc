const I=window.VPMED_INTERACTIONS||[],D=window.VPMED_DRUGS||[],$=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const n=s=>s.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,' ').trim();const aliases={};D.forEach(x=>{aliases[n(x.brand)]=x.active;aliases[n(x.active)]=x.active});function parts(name){return name.split(/\s*[+\-]\s*/).map(x=>x.trim()).filter(Boolean)}function canonical(v){return aliases[n(v)]||v.trim()}function matchPair(a,b,i){const p=parts(i.name).map(canonical).map(n);const x=n(canonical(a)),y=n(canonical(b));return p.length>=2&&((p[0].includes(x)||x.includes(p[0]))&&(p[1].includes(y)||y.includes(p[1]))||(p[0].includes(y)||y.includes(p[0]))&&(p[1].includes(x)||x.includes(p[1])))}const meds=[...new Set([...D.flatMap(x=>[x.brand,x.active]),...I.flatMap(x=>parts(x.name))])].sort();$('#meds').innerHTML=meds.map(x=>`<option value="${esc(x)}">`).join('');$('#icount').textContent=I.length;$('#mcount').textContent=new Set(I.flatMap(x=>parts(x.name).map(n))).size;function card(x){return `<div class="int"><b>${esc(x.name)}</b><span style="float:right;color:#0871b4;font-size:12px">Nhấp để xem chi tiết →</span><div class="detail"><p><b>Mức độ:</b> ${esc(x.level)}</p><p><b>Hậu quả:</b> ${esc(x.consequence)}</p><p><b>Cơ chế:</b> ${esc(x.mechanism)}</p><p><b>Cách xử trí:</b> ${esc(x.management)}</p><p><b>Nguồn:</b> ${esc(x.source)}</p></div></div>`}function render(){const q=n($('#iq').value);const f=I.filter(x=>!q||n(Object.values(x).join(' ')).includes(q));$('#ilist').innerHTML=f.map(card).join('')||'<div class="warn">Không có kết quả.</div>';document.querySelectorAll('.int').forEach(e=>e.onclick=()=>e.classList.toggle('open'))}$('#iq').oninput=render;render();$('#swap').onclick=()=>{const t=$('#a').value;$('#a').value=$('#b').value;$('#b').value=t};$('#check').onclick=()=>{const a=$('#a').value,b=$('#b').value;if(!a||!b){$('#result').innerHTML='Vui lòng nhập đủ hai thuốc.';return}const m=I.filter(x=>matchPair(a,b,x));$('#result').innerHTML=m.length?m.map(card).join(''):'<b>Chưa tìm thấy cặp tương tác trong danh mục hiện có.</b><br>Điều này không khẳng định phối hợp an toàn; cần kiểm tra thêm tờ HDSD và nguồn chuyên môn.';$('#result').querySelectorAll('.int').forEach(e=>e.onclick=()=>e.classList.toggle('open'))};
