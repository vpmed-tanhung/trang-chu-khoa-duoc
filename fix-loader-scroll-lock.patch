diff --git a/assets/system-loader.css b/assets/system-loader.css
index c9d5514..1f416d4 100644
--- a/assets/system-loader.css
+++ b/assets/system-loader.css
@@ -3,22 +3,35 @@ html.system-loading,
 html.system-loading body {
   overflow: hidden !important;
   overscroll-behavior: none;
+  height: 100%;
 }
 
 #systemLoader.system-loader {
   position: fixed;
   inset: 0;
+  top: 0;
+  right: 0;
+  bottom: 0;
+  left: 0;
   z-index: 1000000;
   display: grid;
   place-items: center;
-  min-height: 100vh;
-  min-height: 100dvh;
+  width: 100vw;
+  height: 100vh;
+  height: 100dvh;
+  height: -webkit-fill-available;
   padding: 20px;
-  background:
+  /* Nền đặc trước, gradient trang trí phủ lên trên - đảm bảo không có khe hở
+     lộ giao diện thật kể cả khi trình duyệt vẽ gradient chậm hơn 1 khung hình. */
+  background-color: #eaf5fc;
+  background-image:
     radial-gradient(circle at 50% 4%, rgba(83, 180, 235, .24), transparent 33%),
     linear-gradient(160deg, #f7fbff 0%, #eaf5fc 52%, #f8fbfd 100%);
   opacity: 1;
   visibility: visible;
+  isolation: isolate;
+  transform: translateZ(0);
+  -webkit-transform: translateZ(0);
   transition: opacity .42s ease, visibility .42s ease;
 }
 
diff --git a/assets/system-loader.js b/assets/system-loader.js
index c1b9bd9..203a94a 100644
--- a/assets/system-loader.js
+++ b/assets/system-loader.js
@@ -3,6 +3,7 @@
   'use strict';
 
   var root = document.documentElement;
+  var body = document.body;
   var loader = document.getElementById('systemLoader');
   var bar = document.getElementById('systemLoaderBar');
   var status = document.getElementById('systemLoaderStatus');
@@ -13,6 +14,28 @@
     return;
   }
 
+  /* Khóa cuộn trang thật trên iOS Safari: overflow:hidden không đủ để chặn
+     cuộn nảy đàn hồi (rubber-band), khiến overlay bị kéo lệch và lộ giao diện
+     bên dưới. Cố định luôn <body> tại đúng vị trí cuộn hiện tại. */
+  var lockScrollY = window.scrollY || window.pageYOffset || 0;
+  if (body) {
+    body.style.position = 'fixed';
+    body.style.top = (-lockScrollY) + 'px';
+    body.style.left = '0';
+    body.style.right = '0';
+    body.style.width = '100%';
+  }
+
+  function unlockScroll() {
+    if (!body) return;
+    body.style.position = '';
+    body.style.top = '';
+    body.style.left = '';
+    body.style.right = '';
+    body.style.width = '';
+    window.scrollTo(0, lockScrollY);
+  }
+
   var startedAt = Date.now();
   var minimumVisibleMs = 1800;
   var closed = false;
@@ -40,6 +63,7 @@
         closed = true;
         loader.classList.add('is-hidden');
         root.classList.remove('system-loading');
+        unlockScroll();
 
         window.setTimeout(function () {
           if (loader.parentNode) loader.parentNode.removeChild(loader);
